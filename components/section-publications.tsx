export default function SectionPublications() {
  return (
    <div
      css={{
        fontWeight: 500,
        fontSize: 16,
      }}
    >
      {`Coming soon. (The list, not the publications).`}
    </div>
  );
}
